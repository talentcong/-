"""实验报告正文内容。

每个元素为 (类型, 文本)：'p' 正文段落、'blank' 空行、'image' 插图。
"""

CONTENT = [
    ("p", "项目名称：听障无障碍环境声音提示智能体——基于 YAMNet 零样本声学事件检测与 Coze 智能体"),
    ("p", "学生姓名："),
    ("blank", ""),

    ("p", "一、实验目的"),
    ("p", "1. 了解听障人群在环境声音感知上的障碍，认识无障碍 AI 辅助技术的应用价值与技术定位。"),
    ("p", "2. 掌握声学事件检测（Sound Event Detection, SED）的基本概念，理解其与语音识别（ASR）的本质区别。"),
    ("p", "3. 掌握 AudioSet 本体与 YAMNet 预训练模型的原理，理解零样本迁移（zero-shot transfer）的工作方式与适用边界。"),
    ("p", "4. 掌握 ONNX Runtime 推理部署方法，在不依赖 PyTorch / TensorFlow 的前提下完成深度模型推理。"),
    ("p", "5. 设计并实现「音频输入 → 事件识别 → 紧急分级 → 提示文案」的完整链路，完成工作流方案设计。"),
    ("p", "6. 分析零样本路线的技术局限、类别覆盖盲区与相关伦理风险。"),
    ("blank", ""),

    ("p", "二、实验原理"),
    ("p", "2.1 声学事件检测与语音识别的区别"),
    ("p", "语音识别（ASR）处理的对象是人的语音，目标是转写出文字内容；声学事件检测（SED）处理的对象是环境中非语音的声音——门铃、火警、玻璃破碎、婴儿啼哭等，目标是判断「发生了什么声音、在什么时候发生」。"),
    ("p", "对听障人群而言，真正构成安全风险的信息大多不在语音里，而在环境声中。因此本实验的技术路线是 SED，而不是 ASR。这是本实验与常规音频处理实验在选题上的根本差异。"),
    ("p", "2.2 AudioSet 本体"),
    ("p", "AudioSet 是 Google 发布的大规模音频事件数据集，其类别体系（ontology）为层级结构，平铺后共 521 个类别。本实验使用的类别表 yamnet_class_map.csv 即对应这套体系，字段为 index、mid、display_name。"),
    ("p", "AudioSet 覆盖了火警、警笛、枪声、玻璃破碎、门铃、敲门、婴儿哭、狗叫、汽车鸣笛等与本实验目标高度相关的类别，这是选用它作为零样本识别基础的前提。"),
    ("p", "2.3 YAMNet 模型"),
    ("p", "YAMNet 是 Google 基于 MobileNetV1 深度可分离卷积架构训练的音频事件分类模型，在 AudioSet 上预训练，输出 521 类的多标签得分。模型输入为 16 kHz 单声道波形，内部完成分帧与梅尔谱计算，帧长约 0.96 秒、帧移约 0.48 秒。"),
    ("p", "本实验使用的是从社区镜像获取的 ONNX 格式权重（niobures/YAMNet 仓库的 yamnetonnx/yamnet.onnx，16,124,200 字节）。经 scripts/probe_model.py 实测，其输入输出规格为："),
    ("p", "输入 waveform，形状 [unk__413]，为动态长度的一维 float32 波形；"),
    ("p", "输出 output_0，形状 [unk__414, 521]，为逐帧的 AudioSet 得分矩阵；"),
    ("p", "另有 output_1（[unk, 1024] 嵌入向量）与 output_2（[unk, 64] 梅尔谱）两个辅助输出，本实验未使用。"),
    ("p", "2.4 零样本迁移与 ONNX 推理"),
    ("p", "本实验不训练也不微调任何模型，直接使用 YAMNet 在 AudioSet 上的原始输出，通过「类别映射层」把 521 类得分归并、分级为听障关注事件。这种做法的优点是不需要标注数据与训练算力，缺点是识别精度受限于 AudioSet 本体与实际场景的匹配程度。"),
    ("p", "部署侧采用 ONNX Runtime。相比 PyTorch / TensorFlow，ONNX Runtime 安装体积小、无需深度学习框架依赖，在本实验的推理规模下完全够用，也便于在普通笔记本上复现。"),
    ("p", "2.5 映射层设计"),
    ("p", "映射层是本实验的技术核心，需要解决三个问题："),
    ("p", "一是紧急度分级。按危险程度分为三级：一级为危险、需要立即提示（火警、警报声、爆炸声、枪声、玻璃破碎、尖叫声等）；二级为注意、需要知晓（门铃、敲门、电话铃声、婴儿哭声、狗叫声、汽车鸣笛等）；三级为已识别但可忽略的环境音（音乐、说话声、交通噪声、家电运行声等）。"),
    ("p", "二是多类归并。AudioSet 中同一物理事件常分布在多个类别，例如 Smoke detector, smoke alarm 与 Fire alarm 属于同一事件族，Siren、Civil defense siren、Police car (siren)、Ambulance (siren) 同属警报族。若不做归并，同一声源会触发多条重复提示。"),
    ("p", "三是误报控制。单帧 argmax 在连续音频上抖动严重，需要通过置信度阈值与多帧一致性两个手段抑制瞬时误报。这两个参数的取值直接决定系统的可用性，是本实验第五章的主要观测对象。"),
    ("p", "2.6 覆盖盲区的定义"),
    ("p", "映射层中，未被任何规则覆盖的类别被显式标记为「未覆盖」（level = 0），而不是简单地归入「可忽略」。这一区分是本实验刻意设计的：可忽略表示「已识别且确认无需提示」，未覆盖表示「AudioSet 本体中不存在该声音」。两者的性质完全不同，后者反映的是技术路线本身的局限，必须单独统计。"),
    ("blank", ""),

    ("p", "三、实验环境"),
    ("p", "3.1 软件平台"),
    ("p", "本地识别链路：Python 3.13.3，Windows 11。主要依赖为 onnxruntime 1.30.0（推理引擎）、numpy 2.4.6、scipy 1.17.1（数值计算）、matplotlib（绘图）、pytest（测试）。"),
    ("p", "其中 onnxruntime、numpy、scipy 三个直接决定实验数值结果的包在 requirements.txt 中精确锁定版本，以保证结果可复现；matplotlib、python-docx、pytest 不参与数值计算，使用下限约束。"),
    ("p", "提示生成链路：Coze 扣子网页端，智能体工作流编辑器。"),
    ("p", "3.2 模型与数据来源"),
    ("p", "模型：YAMNet ONNX 权重，经 hf-mirror.com 镜像下载，16,124,200 字节。"),
    ("p", "评估数据：ESC-50 环境声音数据集（Piczak, K. J. ESC: Dataset for Environmental Sound Classification. ACM MM 2015），从 GitHub 原始文件地址逐个下载其后 13 个与听障提示相关的类别、每类 5 条，共 65 条片段，每条为 16-bit 单声道 44.1 kHz WAV、时长 5 秒、441,044 字节。"),
    ("p", "3.3 网络环境约束"),
    ("p", "实验过程中遇到两处网络限制，均已实测确认并写入代码处理："),
    ("p", "一是 huggingface.co 在本机直连不通（HTTP 000），模型下载必须走 hf-mirror.com 镜像（实测可达）。"),
    ("p", "二是 raw.githubusercontent.com 的 DNS 解析返回 4 个 IPv6 地址与 4 个 IPv4 地址，其中 3 个 IPv6 地址实测不可达；Python 的 urllib 未实现 Happy Eyeballs（RFC 8305）地址竞速，会按顺序尝试并卡在不可达的 IPv6 上直到超时，表现为间歇性失败。代码中通过 _force_ipv4() 限定解析只返回 IPv4 解决。"),
    ("p", "3.4 控制台编码"),
    ("p", "本机 Git Bash 的 stdout 默认编码为 GBK，脚本输出的中文会显示为乱码。运行需设置 PYTHONIOENCODING=utf-8，或在 Windows 终端中切换代码页为 UTF-8。落盘的 JSON 文件统一以 UTF-8 写入，不受此影响。"),
    ("p", "3.5 输出形式"),
    ("p", "事件 JSON（含事件名、紧急等级、置信度、可追溯的 AudioSet 原始类别名）、命中率明细 CSV、按类汇总 JSON、混淆矩阵 CSV、参数扫描对比图 PNG、Coze 提示文案。"),
    ("blank", ""),

    ("p", "四、实验内容与方案设计"),
    ("p", "本实验拆分为三个前后衔接的场景：场景一完成「音频到得分」，场景二完成「得分到事件」，场景三完成「事件到提示文案」。"),
    ("blank", ""),

    ("p", "实验场景1：YAMNet 零样本环境声音识别"),
    ("p", "1.1 实验目的"),
    ("p", "实现从环境声音 WAV 文件到 521 类 AudioSet 逐帧得分的完整推理链路，验证 ONNX 模型在本机可正常加载与推理，并确认模型的输入输出规格。"),
    ("p", "1.2 使用组件"),
    ("p", "自建模块：audio_io.py（音频读取与预处理）、yamnet_runner.py（ONNX 推理封装）、probe_model.py（模型规格探测）。"),
    ("p", "第三方库：onnxruntime 1.30.0、numpy、scipy。不使用 PyTorch 或 TensorFlow。"),
    ("p", "1.3 实现步骤"),
    ("p", "1）调用 probe_model.py 探测模型规格，打印输入输出名称、形状与类型，并用 1 秒与 5 秒的合成正弦信号各跑一次推理，确认输出帧数随输入长度变化。"),
    ("p", "2）读取音频文件：统一转换为 float32、转单声道、用 scipy.signal.resample_poly 重采样到 16 kHz、峰值归一化到 [-1, 1]。"),
    ("p", "3）把波形送入 ONNX 会话推理，取出第一个输出即 (n_frames, 521) 的得分矩阵。"),
    ("p", "4）对得分矩阵按帧轴取均值，得到 521 维的聚合得分，作为场景二的输入。"),
    ("p", "1.4 输入输出"),
    ("p", "输入：16-bit PCM WAV 音频文件，采样率不限（内部统一重采样至 16 kHz）。"),
    ("p", "输出：(n_frames, 521) 的 float32 得分矩阵；聚合后为 521 维得分向量。"),
    ("p", "实测：1.0 秒音频输出 2 帧，5.0 秒音频输出 10 帧，与 YAMNet 的帧长 0.96 秒、帧移 0.48 秒相符，且证明模型支持变长输入，无需补零或截断。"),
    ("p", "1.5 注意事项"),
    ("p", "一是必须使用 16 kHz 单声道。YAMNet 的预处理在计算图内部完成，外部传入立体声或非 16 kHz 波形不会报错，但会得到错误结果，这一点容易疏漏。"),
    ("p", "二是峰值归一化对静音输入要单独处理，否则会除以零。"),
    ("p", "三是模型文件较大（16 MB），已通过 .gitignore 排除出版本控制，需由下载脚本重新获取。"),
    ("blank", ""),

    ("p", "实验场景2：AudioSet 到听障关注事件的映射与分级决策"),
    ("p", "2.1 实验目的"),
    ("p", "把 YAMNet 输出的 521 类原始得分，转换为具有紧急等级、可用于提示的事件列表，并建立误报抑制机制。"),
    ("p", "2.2 使用组件"),
    ("p", "自建模块：event_mapping.py（映射表、分级、覆盖率统计）、decision.py（帧聚合、阈值判定、多帧去抖）。"),
    ("p", "2.3 实现步骤"),
    ("p", "1）建立事件族映射表。按「事件族 → (中文名, 等级, [AudioSet 类别索引])」的结构定义 23 个事件族，共覆盖 53 个 AudioSet 类别。"),
    ("p", "2）建立可忽略类别表，并对未列举类别使用关键词匹配（music、speech、traffic、engine 等 22 个关键词）。"),
    ("p", "3）构建查找表：优先级为「事件表 > 忽略表 > 关键词匹配 > 未覆盖」。"),
    ("p", "4）对聚合得分做阈值判定，低于阈值的类别不产生事件；同一事件族内取最高置信度，避免重复提示。"),
    ("p", "5）统计覆盖率：分别计量一级、二级、可忽略、未覆盖四类各占多少。"),
    ("p", "2.4 输入输出"),
    ("p", "输入：521 维聚合得分向量（或 (n_frames, 521) 帧级得分矩阵，用于多帧去抖）。"),
    ("p", "输出：事件列表，每项含 event_id、中文名、紧急等级、置信度、触发该事件的 AudioSet 原始类别名。"),
    ("p", "2.5 注意事项"),
    ("p", "一是映射表中的类别索引必须与实际下载的 yamnet_class_map.csv 逐条核对，不能凭记忆或文献填写；索引错位会导致事件名与声音完全对不上，且不会报错。"),
    ("p", "二是音频事件检测的置信度普遍偏低。YAMNet 输出的是多标签 sigmoid 得分而非 softmax，521 类分摊后单类得分常在 0.1 至 0.5 之间。若沿用常见的 0.5 阈值，会把大量正确识别一并滤除，这是本实验第五章参数扫描要量化的核心问题。"),
    ("p", "三是「未覆盖」必须与「可忽略」分开统计，否则会掩盖 AudioSet 本体对家居安全声音覆盖不足的事实。"),
    ("blank", ""),

    ("p", "实验场景3：Coze 智能体分级提示文案生成"),
    ("p", "3.1 实验目的"),
    ("p", "把结构化的事件 JSON 转换为面向听障用户的自然语言提示文案，实现对检测结果的可读化输出。"),
    ("p", "3.2 使用组件"),
    ("p", "Coze 扣子平台：工作流（开始节点 → 大模型节点 → 结束节点）、大模型节点（豆包系列）。"),
    ("p", "衔接方式：人工把本地生成的事件 JSON 粘贴进工作流输入。"),
    ("p", "3.3 实现步骤"),
    ("p", "1）本地运行 detect.py 生成事件 JSON，格式见 3.4。"),
    ("p", "2）在 Coze 新建工作流，节点结构为「开始节点 → 大模型节点 → 结束节点」。"),
    ("p", "3）在大模型节点中填入系统提示词，温度设为 0.3 以抑制语言发散。"),
    ("p", "4）依次粘贴三份代表性 JSON：一份一级事件、一份二级事件、一份无事件。"),
    ("p", "5）记录每次输出的提示文案，并核对模型是否遵守了提示词中的各项约束。"),
    ("p", "3.4 输入输出"),
    ("p", "输入：事件 JSON，字段为 session_id、audio_file、duration_sec、max_level（本次最高紧急等级）与 events 数组；events 每项含 event_id、name_cn、level、level_name、confidence、audio_set_labels。"),
    ("p", "输出：1 至 3 句中文提示文案。"),
    ("p", "3.5 注意事项"),
    ("p", "一是提示词中必须明确禁止推测未出现的声音。大模型在没有约束时倾向于补全场景，会把「检测到门铃」扩写成「有人来访，请开门」，而后者是未经检测的推断，可能误导用户。"),
    ("p", "二是低置信度事件必须用「可能」「似乎」等措辞弱化，不能表述为确定发生。"),
    ("p", "三是面向听障用户应避免「置信度」「模型」等术语，表达要直白，适合快速阅读。"),
    ("p", "四是链路衔接为人工粘贴，未调用 Coze API。若需自动化，可改为通过 Coze 开放接口由本地脚本直接投递。"),
    ("blank", ""),

    ("p", "五、实验结果与分析"),
    ("p", "5.1 管线连通性验证"),
    ("p", "使用 scripts/make_test_signals.py 生成 6 个合成信号（440 Hz 正弦、1000 Hz 正弦、100 Hz–8 kHz 扫频、白噪声、静音、4 Hz 脉冲串），在阈值 0.2 下运行 detect.py，结果如下："),
    ("p", "tone_440hz.wav：电话铃声 0.97、警报器 0.81；"),
    ("p", "tone_1000hz.wav：电子提示音 0.98；"),
    ("p", "sweep_100_8000.wav：警报器 0.21；"),
    ("p", "white_noise.wav、silence.wav、pulses_4hz.wav：无提示事件。"),
    ("p", "440 Hz 被识别为电话铃声具有合理性——电话拨号音的标准频率正是 440 Hz，说明模型确实在依据频谱特征做出判断，而非随机输出。静音与白噪声未触发任何提示，说明阈值机制在无明确事件时能保持静默。"),
    ("p", "需要说明的是，合成信号只用于验证管线连通性与响应方向，不能用于评估识别准确率。"),
    ("p", "5.2 映射覆盖率分析"),
    ("p", "对实际下载的 yamnet_class_map.csv 全部 521 个类别执行映射，统计结果如下："),
    ("p", "一级（危险）：23 类，占 4.4%；"),
    ("p", "二级（注意）：30 类，占 5.8%；"),
    ("p", "三级（可忽略）：94 类，占 18.0%；"),
    ("p", "未覆盖：374 类，占 71.8%。"),
    ("p", "这是本次实验最重要的发现之一：即便把关键词匹配放宽，仍有 71.8% 的 AudioSet 类别无法归入任何听障提示场景。这并非映射表编写不足，而是 AudioSet 本体设计取向所致——它面向通用音频事件识别，包含大量乐器、音乐流派、动物物种等与听障安全无关的细分类别。"),
    ("p", "反过来看，这个数字量化了零样本路线的能力上限：系统的可提示范围被 AudioSet 本体预先决定，无法通过调参突破。像烟雾报警器、门铃这类关键声音在 AudioSet 中存在对应类别，因此可用；但更多居家安全声音（如燃气报警、水壶鸣笛、某些家电异响）在本体中没有独立类别，只能落入未覆盖。这是技术路线的结构性局限，不是实现缺陷。"),
    ("p", "5.3 事件级命中率"),
    ("p", "使用 ESC-50 抽样的 65 条片段（13 个类别 × 5 条）做评估。"),
    ("p", "指标口径说明：ESC-50 的类别体系与 AudioSet 不同，无法做标准分类准确率对比。本实验统计的是「零样本预测是否落进该类别应有的听障事件族」，即事件级命中率。该数字不可与文献中的分类准确率直接比较。"),
    ("p", "对应当前参数（阈值 0.2），事件级命中率为 60.0%（39/65）。"),
    ("p", "5.4 阈值与去抖参数对比"),
    ("p", "阈值扫描（min_frames = 1）："),
    ("p", "阈值 0.05：76.9%（50/65）；阈值 0.10：70.8%（46/65）；阈值 0.20：60.0%（39/65）；阈值 0.30：53.8%（35/65）；阈值 0.50：43.1%（28/65）。"),
    ("p", "命中率随阈值单调下降，与 2.5 节的预判一致。0.5 这一在分类任务中常见的默认阈值，会使命中率比 0.05 低 33.8 个百分点。原因是 YAMNet 输出多标签 sigmoid 得分，521 类分摊后单类得分天然偏低，高阈值会连同正确识别一并滤除。"),
    ("p", "去抖窗口扫描（阈值固定 0.10）："),
    ("p", "min_frames = 1：70.8%；min_frames = 2：72.3%；min_frames = 3：64.6%。"),
    ("p", "窗口从 1 帧放宽到 2 帧时命中率略有提升（+1.5 个百分点），说明双帧一致性确实能滤除部分瞬时误报；继续放宽到 3 帧则明显下降（-7.7 个百分点），因为 5 秒片段仅含约 10 帧，要求 3 帧命中会滤掉真实但短暂的声音事件。"),
    ("p", "参数选择建议：若以召回优先（不错过危险声音），取阈值 0.05 至 0.10、min_frames = 2；若以抑制误报优先（避免打扰用户），取阈值 0.20 至 0.30。本实验认为听障安全场景应以召回优先——漏报火警的代价远高于多提示一次门铃。"),
    ("p", "参数扫描对比图如下："),
    ("image", "threshold_sweep.png"),
    ("p", "5.5 误判案例分析"),
    ("p", "在低阈值（0.1）下，闹钟片段 5-210612-A-37 触发了一级事件「火警」（置信度 0.34）与「警报器」（0.38），而同片段同时给出了二级事件「闹钟」（0.55）。这是一次典型误报：闹钟的周期性蜂鸣在频谱特征上与火警报警声高度相似，模型给出的多个类别得分都超过了低阈值。"),
    ("p", "该案例说明两点：一是低阈值确实以误报率换召回率，需要按场景权衡；二是 AudioSet 中「Alarm」「Buzzer」「Beep, bleep」「Alarm clock」「Fire alarm」等类别在声学上相互交叠，仅靠类别映射难以完全区分，需要在映射层之上再引入时序模式判别（如周期性检测）才能进一步改善。"),
    ("p", "此外，部分片段（如敲门声、玻璃破碎）在零样本设置下未被检出，原因是这些声音在模型训练分布中样本较少，单类得分普遍低于阈值。这类漏报在安全场景中后果严重，是本路线最需要警惕的失败模式。"),
    ("blank", ""),

    ("p", "六、实验伦理与风险讨论"),
    ("p", "1. 漏报与误报的安全后果不对称。本系统的输出会直接影响听障用户对环境的判断，因此两类错误不可等价看待：漏报火警可能导致人身伤害，误报则在长期使用中造成「提示疲劳」，使用户对提示失去信任而忽略真实警报。工程上必须明确设计取向，并在产品说明中如实告知用户系统的能力边界，避免其形成过度依赖。"),
    ("p", "2. 持续监听的隐私风险。真实部署该功能需要麦克风持续采集环境音频，可推断用户在家时长、作息、来访情况等敏感信息。必须做到本地推理、音频不外传、不上云、不留存原始录音。本实验为离线批处理，不涉及此问题，但方案设计时必须考虑。"),
    ("p", "3. 数据集与模型的偏见。AudioSet 的类别分布与录制场景以欧美城市环境为主，对特定地区的家居声音覆盖不足，前文 71.8% 的未覆盖率正反映了这一点。直接部署可能出现在特定居住环境中大量关键声音无法识别的情况，且这种失效是静默的——用户不会知道系统「听不到」什么。"),
    ("p", "4. ESC-50 数据许可与署名。本实验使用的 ESC-50 数据集采用 CC BY-NC 3.0（署名—非商业性使用）许可。本实验仅用于课程教学，不商用、不再分发，并在报告中署名原作者：Piczak, K. J. ESC: Dataset for Environmental Sound Classification. ACM MM 2015。任何商业用途均需另行获得授权。"),
    ("p", "5. 责任边界。本实验的定位是技术验证，不具备任何安全设备的资质，不可作为火灾报警、安防监控等场景的替代或补充。若用于真实辅助，需明确其为「辅助提示」而非「安全保障」，并将最终判断权保留给用户。"),
    ("blank", ""),

    ("p", "七、实验提交要求"),
    ("p", "1、实验报告选用通用模板，写清详细的搭建过程，可配图和代码，对实验结果有一定的分析。"),
    ("p", "2、实验成果，录运行演示视频，并提交输出结果。如工作流、软件等可访问，则提供链接或者打包代码。"),
    ("blank", ""),

    ("p", "八、思考题"),
    ("p", "1. 本次开发有什么技术局限或者伦理风险？"),
    ("p", "技术局限主要有三。其一，零样本路线决定了能力上限：系统能识别什么，完全由 AudioSet 本体的 521 类预先决定，实测有 71.8% 的类别无法映射到听障场景，且像燃气报警、水壶鸣笛等居家安全声音在本体中没有独立类别，无法通过调参弥补。其二，AudioSet 中 Alarm、Buzzer、Beep、Alarm clock、Fire alarm 等类别声学特征高度重叠，仅靠类别映射难以区分，实测中出现过闹钟被误报为火警的案例。其三，零样本模式下部分关键声音（敲门、玻璃破碎）单类得分偏低而被漏检，这类漏报若不处理会严重削弱系统可信度。"),
    ("p", "伦理风险方面，最突出的是漏报与误报后果的不对称性，以及长期误报导致的提示疲劳；其次是持续麦克风采集带来的隐私暴露；第三是数据集地域偏见造成特定环境下静默失效，而用户对此无从知晓。因此应当把系统定位为辅助提示而非安全保障。"),
    ("p", "2. 你还能想到哪些包含音频处理模块的应用？"),
    ("p", "一是工业设备预测性维护，通过对机械运转声的异常检测提前发现故障，技术路线与本实验的事件检测高度相通。二是城市噪声监测与声环境地图，用分布式麦克风节点识别交通、施工、生活噪声并绘制时空分布。三是野生动物声学监测，通过识别特定物种鸣叫评估生物多样性。四是居家养老看护，识别跌倒声、呼救声、长时间无活动等异常。五是会议与课堂的说话人分离与纪要生成，属于语音处理方向。六是智能耳机的通透模式，根据环境声类型动态调整降噪策略。"),
    ("p", "3. 你想重点学习哪些数字音频处理技术？"),
    ("p", "其一，时频分析与特征提取，包括短时傅里叶变换、梅尔滤波器组、MFCC，希望从本次「直接调用预训练模型」进一步下沉到自己实现特征提取，理解模型的输入到底是什么。其二，数据增强与少样本学习，用于解决本次暴露的漏检问题——在标注数据稀缺的类别上提升识别能力。其三，时序建模与声事件起止检测，本次只做了片段级分类，无法给出事件在时间轴上的准确边界，而这正是 SED 相对分类的完整形态。其四，模型轻量化与端侧部署，本实验用 ONNX Runtime 跑在笔记本上，若要真正做成便携辅具，需要进一步量化与优化。"),
    ("blank", ""),
]
