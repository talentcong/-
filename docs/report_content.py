"""实验报告正文内容。

每个元素为 (类型, 文本)：'p' 正文段落、'blank' 空行、'image' 插图、'link' 超链接。
标题由 build_report.py 按正则识别，套用宋体四号；其余套用仿宋小四。
"""

CONTENT = [
    ("p", "项目名称：听障无障碍环境声音提示智能体——基于 YAMNet 零样本声学事件检测与 Coze 智能体"),
    ("p", "学生姓名："),
    ("blank", ""),

    ("p", "一、实验目的"),
    ("p", "1. 了解听障人群的环境声音感知障碍，认识无障碍 AI 辅助技术的应用价值。"),
    ("p", "2. 掌握声学事件检测（SED）的概念，理解其与语音识别（ASR）的本质区别。"),
    ("p", "3. 掌握 AudioSet 本体与 YAMNet 模型的原理，理解零样本迁移的工作方式与边界。"),
    ("p", "4. 掌握 ONNX Runtime 推理部署方法，在不依赖 PyTorch / TensorFlow 的前提下完成模型推理。"),
    ("p", "5. 实现「音频 → 事件识别 → 紧急分级 → 提示文案」完整链路，分析其技术局限与伦理风险。"),
    ("blank", ""),

    ("p", "二、实验原理"),
    ("p", "2.1 声学事件检测与语音识别的区别"),
    ("p", "语音识别转写语音，声学事件检测判断非语音的环境声。听障人群的安全风险大多不在语音里，故采用 SED 而非 ASR 路线。"),
    ("p", "2.2 AudioSet 本体与 YAMNet 模型"),
    ("p", "AudioSet 是 Google 的大规模音频事件数据集，类别平铺后共 521 类，含火警、警笛、玻璃破碎、门铃、婴儿哭等。YAMNet 是基于 MobileNetV1 的分类模型，在 AudioSet 上预训练，输入 16 kHz 单声道波形，内部完成分帧与梅尔谱计算，输出 521 类多标签得分。"),
    ("p", "实验使用社区镜像的 ONNX 权重（niobures/YAMNet，16,124,200 字节），实测输入为动态长度波形，输出为 [n_frames, 521] 得分矩阵。"),
    ("p", "2.3 零样本迁移与映射层"),
    ("p", "不训练也不微调，直接用 YAMNet 原始输出经映射层归并为听障事件。优点是无需标注数据与算力，缺点是精度受限于 AudioSet 与实际场景的匹配度。部署用 ONNX Runtime，体积小、无框架依赖。"),
    ("p", "映射层解决三个问题。一是分级：一级危险（火警、警报声、爆炸声、玻璃破碎等），二级注意（门铃、敲门、婴儿哭、狗叫等），三级可忽略（音乐、说话声、交通噪声等）。二是归并：同一事件常分布在多个类别（如 Smoke detector 与 Fire alarm 同属火警族），不归并会重复提示。三是误报控制：用置信度阈值与多帧一致性抑制抖动，这两个参数是第五章的观测重点。"),
    ("p", "未覆盖的类别标记为「未覆盖」而非「可忽略」：前者表示本体不含该声音，后者表示已识别且确认无需提示。该区分用于量化路线的能力边界。"),
    ("blank", ""),

    ("p", "三、实验环境"),
    ("p", "3.1 软件平台"),
    ("p", "本地识别：Python 3.13.3、Windows 11，依赖 onnxruntime 1.30.0、numpy 2.4.6、scipy 1.17.1、matplotlib、pytest，前三个精确锁定以保证可复现。提示生成：Coze 网页端；原计划用工作流编辑器，实测该版本未提供经典画布入口，改在对话界面验证，详见场景三。"),
    ("p", "3.2 模型与数据来源"),
    ("p", "模型经 hf-mirror.com 下载。评估数据为 ESC-50（Piczak, K. J. ACM MM 2015），取 13 个听障相关类别、每类 5 条共 65 条，每条 16-bit 单声道 44.1 kHz WAV、5 秒。"),
    ("p", "3.3 网络约束与控制台编码"),
    ("p", "两处网络限制：huggingface.co 直连不通，改走 hf-mirror.com；raw.githubusercontent.com 的 3 个 IPv6 地址不可达，而 urllib 未实现 Happy Eyeballs 竞速会卡到超时，代码用 _force_ipv4() 限定只解析 IPv4。此外本机 stdout 默认 GBK，中文输出需设 PYTHONIOENCODING=utf-8；落盘 JSON 统一 UTF-8。"),
    ("blank", ""),

    ("p", "四、实验内容与方案设计"),
    ("p", "实验分三个衔接场景：场景一「音频到得分」、场景二「得分到事件」、场景三「事件到提示文案」。"),
    ("blank", ""),

    ("p", "实验场景1：YAMNet 零样本环境声音识别"),
    ("p", "1.1 实验目的"),
    ("p", "实现从 WAV 到 521 类逐帧得分的推理链路，确认模型输入输出规格。"),
    ("p", "1.2 使用组件"),
    ("p", "audio_io.py（预处理）、yamnet_runner.py（推理封装）、probe_model.py（规格探测）；依赖 onnxruntime、numpy、scipy，不使用 PyTorch/TensorFlow。"),
    ("p", "1.3 实现步骤"),
    ("p", "探测规格；读音频转 float32、单声道、重采样 16 kHz、峰值归一化；送入 ONNX 取 (n_frames,521) 矩阵；按帧均值聚合为 521 维。"),
    ("p", "1.4 输入输出"),
    ("p", "输入 16-bit PCM WAV；输出 (n_frames,521) 矩阵。实测 1.0 秒出 2 帧、5.0 秒出 10 帧，与帧长 0.96 秒、帧移 0.48 秒相符，证明支持变长输入，无需补零截断。"),
    ("p", "1.5 注意事项"),
    ("p", "必须 16 kHz 单声道——预处理在图内完成，传错采样率或立体声不报错但结果错误。峰值归一化需处理静音以免除零。模型文件 16 MB，已排除出版本控制。"),
    ("blank", ""),

    ("p", "实验场景2：AudioSet 到听障关注事件的映射与分级决策"),
    ("p", "2.1 实验目的"),
    ("p", "把 521 类原始得分转换为带紧急等级、可用于提示的事件列表，并建立误报抑制机制。"),
    ("p", "2.2 使用组件"),
    ("p", "event_mapping.py（映射表、分级、覆盖率统计）、decision.py（帧聚合、阈值判定、多帧去抖）。"),
    ("p", "2.3 实现步骤"),
    ("p", "定义 23 个事件族覆盖 53 个类别；建可忽略类别表并对未列举类别用关键词匹配；查找表优先级为事件表 > 忽略表 > 关键词 > 未覆盖；阈值判定，同族取最高置信度；统计四类覆盖率。"),
    ("p", "2.4 输入输出"),
    ("p", "输入 521 维聚合得分或帧级矩阵；输出事件列表，含标识、中文名、等级、置信度、触发它的 AudioSet 类别名。"),
    ("p", "2.5 注意事项"),
    ("p", "映射表的类别索引必须与实际下载的 yamnet_class_map.csv 逐条核对，错位会导致事件名与声音完全对不上且不报错。另外 YAMNet 输出多标签 sigmoid 得分，521 类分摊后单类常在 0.1 至 0.5 之间，沿用 0.5 阈值会滤除大量正确识别，这是第五章要量化的核心问题。"),
    ("blank", ""),

    ("p", "实验场景3：Coze 智能体分级提示文案生成"),
    ("p", "3.1 实验目的"),
    ("p", "把结构化的事件 JSON 转换为面向听障用户的自然语言提示文案。"),
    ("p", "3.2 使用组件"),
    ("p", "Coze 扣子平台。因经典工作流画布不可达，改在对话界面验证：@扣子 并发送提示词与事件 JSON。"),
    ("p", "3.3 实现步骤"),
    ("p", "本地生成事件 JSON；@扣子 发送提示词全文；依次发送三份代表性 JSON；记录输出并逐条核对 8 条约束。"),
    ("p", "差异说明：对话界面下提示词作为消息发送，而非配置在节点系统提示词栏，温度参数（0.3）也无法设置。故本次验证证明了该提示词能诱导出符合规则的行为，但不等同于工作流 LLM 节点的表现。"),
    ("p", "3.4 输入输出"),
    ("p", "输入事件 JSON（session_id、audio_file、duration_sec、max_level、events；events 每项含 event_id、name_cn、level、level_name、confidence、audio_set_labels），输出 1 至 3 句提示文案。三组实测结果："),
    ("p", "样本 1（max_level=1，五事件）：「听到了闹钟和电子提示音的声音，可能还有蜂鸣器和报警器，也有点像火警的动静。建议您留意一下周围，确认有没有需要警惕的情况，注意安全。」"),
    ("p", "样本 2（max_level=2，狗叫声 0.11）：「好像有狗在叫，但不太确定，您可以留意一下。」"),
    ("p", "样本 3（max_level=0，events 为空）：「当前未检测到需提示的声音。」"),
    ("p", "3.5 注意事项"),
    ("p", "提示词须明确禁止推测未出现的声音，否则会把「检测到门铃」扩写成「有人来访，请开门」这类未经检测的推断。低置信度事件须用「可能」弱化。面向听障用户避免「置信度」「模型」等术语。链路为人工粘贴，未调用 Coze API。"),
    ("blank", ""),

    ("p", "五、实验结果与分析"),
    ("p", "5.1 管线连通性验证"),
    ("p", "6 个合成信号在阈值 0.2 下：440 Hz → 电话铃声 0.97、警报器 0.81；1000 Hz → 电子提示音 0.98；扫频 → 警报器 0.21；白噪声、静音、脉冲串无事件。440 Hz 正是电话拨号音的标准频率，说明模型确实依据频谱判断。合成信号仅验证管线，不评估准确率。"),
    ("p", "5.2 映射覆盖率分析"),
    ("p", "对全部 521 个类别执行映射：一级 23 类（4.4%），二级 30 类（5.8%），三级可忽略 94 类（18.0%），未覆盖 374 类（71.8%）。"),
    ("p", "这是本次最重要的发现之一：即便放宽关键词匹配，仍有 71.8% 的类别无法归入任何听障提示场景。这并非映射表编写不足，而是 AudioSet 本体的设计取向所致——它面向通用音频事件识别，含大量乐器、音乐流派、动物物种等无关类别。该数字量化了零样本路线的能力上限：可提示范围被本体预先决定，无法通过调参突破。烟雾报警器、门铃等有对应类别因而可用，但燃气报警、水壶鸣笛等居家安全声音没有独立类别，只能落入未覆盖。这是路线的结构性局限，不是实现缺陷。"),
    ("p", "5.3 事件级命中率"),
    ("p", "用 ESC-50 抽样的 65 条片段（13 类 × 5 条）评估。指标口径：ESC-50 类别体系与 AudioSet 不同，无法做标准分类准确率对比，本实验统计的是「零样本预测是否落进该类别应有的听障事件族」，即事件级命中率，不可与文献分类准确率直接比较。当前参数（阈值 0.2）下为 60.0%（39/65）。"),
    ("p", "5.4 阈值与去抖参数对比"),
    ("p", "阈值扫描（min_frames = 1）：0.05 → 76.9%（50/65），0.10 → 70.8%（46/65），0.20 → 60.0%（39/65），0.30 → 53.8%（35/65），0.50 → 43.1%（28/65）。命中率随阈值单调下降，与 2.5 节预判一致。0.5 这一分类任务中的常见默认阈值，会使命中率比 0.05 低 33.8 个百分点。"),
    ("p", "去抖窗口扫描（阈值固定 0.10）：1 帧 → 70.8%，2 帧 → 72.3%，3 帧 → 64.6%。放宽到 2 帧略有提升，说明双帧一致性滤除了部分瞬时误报；到 3 帧明显下降，因 5 秒片段仅含约 10 帧，要求 3 帧命中会滤掉真实但短暂的事件。"),
    ("p", "参数建议：召回优先取阈值 0.05 至 0.10、min_frames = 2；抑制误报优先取 0.20 至 0.30。听障安全场景应以召回优先——漏报火警的代价远高于多提示一次门铃。对比图如下："),
    ("image", "chart"),
    ("p", "5.5 误判案例分析"),
    ("p", "低阈值下闹钟片段 5-210612-A-37 同时触发一级事件「火警」（0.34）、「警报器」（0.38）与二级事件「闹钟」（0.55）。这是典型误报：闹钟的周期性蜂鸣在频谱上与火警报警声高度相似。说明低阈值以误报率换召回率，需按场景权衡；且 Alarm、Buzzer、Beep、Fire alarm 等类别声学特征交叠，仅靠类别映射难以区分。此外敲门声、玻璃破碎等片段在零样本下未被检出，因训练分布中样本较少。这类漏报在安全场景中后果严重，是本路线最需警惕的失败模式。"),
    ("p", "5.6 Coze 提示层验证结果"),
    ("p", "逐条核对三组输出对 8 条规则的遵守情况。符合：规则 1（只依据 events 陈述）三组均未出现 JSON 之外的声音；规则 3（置信度低于 0.5 用「可能」）样本 1 中三个低置信度事件用「可能」「有点像」弱化、两个高置信度事件直述，样本 2 的 0.11 用「好像」「不太确定」，区分正确；规则 4、6、7、8 均符合。"),
    ("p", "两处偏差。规则 5 指定的固定字符串「当前环境未检测到需要提示的声音。」未被照抄，实际输出「当前未检测到需提示的声音。」，语义一致但措辞不同——固定文案应改为代码层兜底。规则 2 对 max_level=1 要求「语气明确、直接」，样本 1 实际偏谨慎；该样本五个事件置信度全部低于 0.55，软化在情理上更稳妥，但与字面要求有距离。"),
    ("p", "一个系统级发现：样本 1 正是 5.5 节的误报案例，而 Coze 输出忠实地复述了这个误报。这暴露了端到端链路的结构性风险：提示词第 1 条在保护用户免受幻觉误导的同时，也剥夺了大模型质疑上游检测结果的权限——检测层一旦出错，提示层会以可信的语气把错误传达给用户。结论：提示层不能作为精度兜底，误报抑制必须在检测层解决。验证截图如下："),
    ("image", "coze"),
    ("blank", ""),

    ("p", "六、实验伦理与风险讨论"),
    ("p", "1. 漏报与误报的后果不对称。漏报火警可能致人伤害，误报则造成提示疲劳使用户忽略真实警报。工程上必须明确设计取向并如实告知能力边界。"),
    ("p", "2. 持续监听的隐私风险。真实部署需麦克风持续采集环境音频，可推断用户在家时长、作息、来访情况。必须本地推理、音频不外传不留存。本实验为离线批处理，不涉及此问题。"),
    ("p", "3. 数据集与模型的偏见。AudioSet 类别分布以欧美城市环境为主，5.2 节 71.8% 的未覆盖率正反映这一点。直接部署可能在特定居住环境中大量关键声音无法识别，且失效是静默的——用户不会知道系统「听不到」什么。"),
    ("p", "4. ESC-50 数据许可与署名。ESC-50 采用 CC BY-NC 3.0（署名—非商业性使用）许可，仅用于课程教学，不商用、不再分发，署名原作者 Piczak, K. J.。任何商业用途均需另行获得授权。"),
    ("p", "5. 责任边界。本实验为技术验证，不具备安全设备资质，不可作为火灾报警、安防监控的替代或补充。若用于真实辅助，须明确其为「辅助提示」而非「安全保障」。"),
    ("blank", ""),

    ("p", "七、实验提交要求"),
    ("p", "1、实验报告选用通用模板，写清详细的搭建过程，可配图和代码，对实验结果有一定的分析。"),
    ("p", "2、实验成果，录运行演示视频，并提交输出结果。如工作流、软件等可访问，则提供链接或者打包代码。"),
    ("p", "本项目全部代码、实验数据与报告已托管至 GitHub 公开仓库："),
    ("link", "", "github"),
    ("p", "代码：scripts/ 下 11 个模块——场景一为 audio_io.py、yamnet_runner.py、probe_model.py；场景二为 event_mapping.py、decision.py；场景三为 detect.py；评估与出图为 evaluate.py、plot_results.py；数据获取为 download_model.py、download_samples.py、make_test_signals.py。"),
    ("p", "测试与数据：tests/ 下 10 个测试文件共 65 个用例；data/out/ 下为 metrics.csv、metrics_summary.json、confusion.csv、threshold_sweep.png；阈值扫描在 thr_*/、去抖扫描在 mf_*/、Coze 样本输入在 coze_samples/。"),
    ("p", "文档：设计文档与实施计划见 docs/superpowers/specs/ 与 docs/superpowers/plans/，Coze 验证记录见 docs/coze-paste-samples.md，验证截图见 docs/images/。"),
    ("p", "模型权重（16 MB）与音频素材（约 28 MB）因体积与许可原因未纳入仓库，可分别运行 python scripts/download_model.py 与 python scripts/download_samples.py 重新获取。"),
    ("blank", ""),

    ("p", "八、思考题"),
    ("p", "1. 本次开发有什么技术局限或者伦理风险？"),
    ("p", "技术局限有三。其一，零样本路线决定能力上限：实测 71.8% 的类别无法映射，燃气报警、水壶鸣笛等居家安全声音在本体中没有独立类别，无法通过调参弥补。其二，Alarm、Buzzer、Beep、Fire alarm 等类别声学特征高度重叠，仅靠类别映射难以区分，实测出现过闹钟被误报为火警。其三，部分关键声音单类得分偏低而被漏检。伦理风险方面，最突出的是漏报与误报后果的不对称性及长期误报造成的提示疲劳；其次是持续麦克风采集带来的隐私暴露；第三是数据集地域偏见造成特定环境下静默失效而用户无从知晓。故应把系统定位为辅助提示而非安全保障。"),
    ("p", "2. 你还能想到哪些包含音频处理模块的应用？"),
    ("p", "一是工业设备预测性维护，通过对机械运转声的异常检测提前发现故障。二是城市噪声监测与声环境地图。三是野生动物声学监测，识别物种鸣叫评估生物多样性。四是居家养老看护，识别跌倒声、呼救声。五是会议与课堂的说话人分离与纪要生成。六是智能耳机通透模式，依环境声类型动态调整降噪。"),
    ("p", "3. 你想重点学习哪些数字音频处理技术？"),
    ("p", "其一，时频分析与特征提取，包括短时傅里叶变换、梅尔滤波器组、MFCC，希望从调用预训练模型下沉到自己实现特征提取。其二，数据增强与少样本学习，用于解决本次暴露的漏检问题。其三，时序建模与声事件起止检测，本次只做片段级分类，无法给出事件在时间轴上的边界。其四，模型轻量化与端侧部署，若要真正做成便携辅具需进一步量化优化。"),
    ("blank", ""),
]
