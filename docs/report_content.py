"""实验报告正文内容。

每个元素为 (类型, 文本)：'p' 正文段落、'blank' 空行、'image' 插图。
标题由 build_report.py 按正则识别，套用宋体四号；其余套用仿宋小四。
"""

CONTENT = [
    ("p", "项目名称：听障无障碍环境声音提示智能体——基于 YAMNet 零样本声学事件检测与 Coze 智能体"),
    ("p", "学生姓名："),
    ("blank", ""),

    ("p", "一、实验目的"),
    ("p", "1. 了解听障人群的环境声音感知障碍，认识无障碍 AI 辅助技术的应用价值。"),
    ("p", "2. 掌握声学事件检测（SED）的概念，理解其与语音识别（ASR）的本质区别。"),
    ("p", "3. 掌握 AudioSet 本体与 YAMNet 模型的原理，理解零样本迁移的工作方式与边界。"),
    ("p", "4. 掌握 ONNX Runtime 推理部署方法，在不依赖 PyTorch / TensorFlow 的前提下完成模型推理。"),
    ("p", "5. 实现「音频 → 事件识别 → 紧急分级 → 提示文案」完整链路，分析其技术局限与伦理风险。"),
    ("blank", ""),

    ("p", "二、实验原理"),
    ("p", "2.1 声学事件检测与语音识别的区别"),
    ("p", "语音识别处理人的语音，目标是转写文字；声学事件检测处理非语音的环境声，目标是判断「发生了什么声音」。对听障人群而言，构成安全风险的信息大多不在语音里，因此本实验采用 SED 而非 ASR 路线。"),
    ("p", "2.2 AudioSet 本体与 YAMNet 模型"),
    ("p", "AudioSet 是 Google 发布的大规模音频事件数据集，类别体系平铺后共 521 类，覆盖火警、警笛、玻璃破碎、门铃、婴儿哭等与本实验高度相关的类别。YAMNet 是基于 MobileNetV1 的音频分类模型，在 AudioSet 上预训练，输入 16 kHz 单声道波形，内部完成分帧与梅尔谱计算，输出 521 类多标签得分。"),
    ("p", "本实验使用社区镜像的 ONNX 权重（niobures/YAMNet 的 yamnetonnx/yamnet.onnx，16,124,200 字节）。经 probe_model.py 实测：输入 waveform 为动态长度一维 float32 波形；输出 output_0 为 [n_frames, 521] 得分矩阵。另有嵌入向量与梅尔谱两个辅助输出，本实验未使用。"),
    ("p", "2.3 零样本迁移与 ONNX 推理"),
    ("p", "本实验不训练也不微调模型，直接使用 YAMNet 的原始输出，通过映射层归并为听障关注事件。优点是无需标注数据与训练算力，缺点是精度受限于 AudioSet 与实际场景的匹配程度。部署侧采用 ONNX Runtime，安装体积小、无深度学习框架依赖，在普通笔记本上即可复现。"),
    ("p", "2.4 映射层设计"),
    ("p", "映射层是本实验的技术核心，需解决三个问题。一是紧急度分级：一级为危险、立即提示（火警、警报声、爆炸声、玻璃破碎等），二级为注意、需要知晓（门铃、敲门、婴儿哭、狗叫等），三级为已识别但可忽略的环境音（音乐、说话声、交通噪声等）。二是多类归并：AudioSet 中同一事件常分布在多个类别（如 Smoke detector 与 Fire alarm 同属火警族），不归并会触发重复提示。三是误报控制：通过置信度阈值与多帧一致性抑制瞬时抖动，这两个参数的取值是本实验第五章的观测重点。"),
    ("p", "未被任何规则覆盖的类别被显式标记为「未覆盖」，而非归入「可忽略」。二者性质不同：前者表示 AudioSet 本体不含该声音，后者表示已识别且确认无需提示。这一区分用于量化技术路线的能力边界。"),
    ("blank", ""),

    ("p", "三、实验环境"),
    ("p", "3.1 软件平台"),
    ("p", "本地识别链路：Python 3.13.3，Windows 11。依赖 onnxruntime 1.30.0、numpy 2.4.6、scipy 1.17.1、matplotlib、pytest。其中前三个直接决定数值结果，在 requirements.txt 中精确锁定以保证可复现。"),
    ("p", "提示生成链路：Coze 扣子网页端。原计划使用智能体工作流编辑器，实测当前版本未提供进入经典工作流画布的入口，故改在对话界面完成验证，详见场景三。"),
    ("p", "3.2 模型与数据来源"),
    ("p", "模型经 hf-mirror.com 镜像下载。评估数据为 ESC-50 环境声音数据集（Piczak, K. J. ACM MM 2015），取其中 13 个与听障提示相关的类别、每类 5 条，共 65 条，每条为 16-bit 单声道 44.1 kHz WAV、时长 5 秒。"),
    ("p", "3.3 网络环境约束"),
    ("p", "遇到两处限制，均已实测确认并在代码中处理。一是 huggingface.co 直连不通，模型下载改走 hf-mirror.com 镜像。二是 raw.githubusercontent.com 的 DNS 返回 4 个 IPv6 与 4 个 IPv4 地址，其中 3 个 IPv6 不可达；Python 的 urllib 未实现 Happy Eyeballs 地址竞速，会卡在不可达的 IPv6 上直到超时，表现为间歇性失败，代码中通过 _force_ipv4() 限定只解析 IPv4 解决。"),
    ("p", "3.4 控制台编码"),
    ("p", "本机 Git Bash 的 stdout 默认编码为 GBK，脚本输出的中文显示为乱码，运行需设置 PYTHONIOENCODING=utf-8。落盘的 JSON 统一以 UTF-8 写入，不受影响。"),
    ("blank", ""),

    ("p", "四、实验内容与方案设计"),
    ("p", "本实验拆分为三个衔接场景：场景一完成「音频到得分」，场景二完成「得分到事件」，场景三完成「事件到提示文案」。"),
    ("blank", ""),

    ("p", "实验场景1：YAMNet 零样本环境声音识别"),
    ("p", "1.1 实验目的"),
    ("p", "实现从 WAV 文件到 521 类 AudioSet 逐帧得分的完整推理链路，验证模型可正常加载并确认其输入输出规格。"),
    ("p", "1.2 使用组件"),
    ("p", "自建模块 audio_io.py（音频预处理）、yamnet_runner.py（推理封装）、probe_model.py（规格探测）；第三方库 onnxruntime、numpy、scipy。不使用 PyTorch 或 TensorFlow。"),
    ("p", "1.3 实现步骤"),
    ("p", "1）探测模型规格并打印输入输出形状；2）读取音频，统一转 float32、转单声道、重采样到 16 kHz、峰值归一化；3）送入 ONNX 会话推理，取 (n_frames, 521) 得分矩阵；4）按帧轴取均值，得到 521 维聚合得分。"),
    ("p", "1.4 输入输出"),
    ("p", "输入为 16-bit PCM WAV，采样率不限。输出为 (n_frames, 521) 得分矩阵，聚合后为 521 维向量。实测 1.0 秒音频输出 2 帧、5.0 秒输出 10 帧，与 YAMNet 帧长 0.96 秒、帧移 0.48 秒相符，且证明模型支持变长输入，无需补零截断。"),
    ("p", "1.5 注意事项"),
    ("p", "必须使用 16 kHz 单声道：预处理在计算图内部完成，传入立体声或错误采样率不会报错，但会得到错误结果，这一点容易疏漏。峰值归一化对静音输入需单独处理以免除零。模型文件 16 MB，已通过 .gitignore 排除出版本控制，由下载脚本重新获取。"),
    ("blank", ""),

    ("p", "实验场景2：AudioSet 到听障关注事件的映射与分级决策"),
    ("p", "2.1 实验目的"),
    ("p", "把 521 类原始得分转换为具有紧急等级、可用于提示的事件列表，并建立误报抑制机制。"),
    ("p", "2.2 使用组件"),
    ("p", "自建模块 event_mapping.py（映射表、分级、覆盖率统计）、decision.py（帧聚合、阈值判定、多帧去抖）。"),
    ("p", "2.3 实现步骤"),
    ("p", "1）按「事件族 → (中文名, 等级, [AudioSet 类别索引])」定义 23 个事件族，覆盖 53 个类别；2）建立可忽略类别表，并对未列举类别使用关键词匹配；3）构建查找表，优先级为事件表 > 忽略表 > 关键词 > 未覆盖；4）对聚合得分做阈值判定，同一事件族取最高置信度；5）统计四类覆盖率。"),
    ("p", "2.4 输入输出"),
    ("p", "输入为 521 维聚合得分向量或帧级得分矩阵。输出为事件列表，每项含事件标识、中文名、紧急等级、置信度、触发该事件的 AudioSet 原始类别名。"),
    ("p", "2.5 注意事项"),
    ("p", "映射表中的类别索引必须与实际下载的 yamnet_class_map.csv 逐条核对，不能凭记忆填写；索引错位会导致事件名与声音完全对不上且不报错。另外，YAMNet 输出的是多标签 sigmoid 得分而非 softmax，521 类分摊后单类得分常在 0.1 至 0.5 之间，沿用常见的 0.5 阈值会滤除大量正确识别，这是第五章参数扫描要量化的核心问题。"),
    ("blank", ""),

    ("p", "实验场景3：Coze 智能体分级提示文案生成"),
    ("p", "3.1 实验目的"),
    ("p", "把结构化的事件 JSON 转换为面向听障用户的自然语言提示文案。"),
    ("p", "3.2 使用组件"),
    ("p", "Coze 扣子平台。原设计为工作流（开始节点 → 大模型节点 → 结束节点），实测当前版本项目打开后为对话式工作台，未提供经典工作流画布入口，故验证改在对话界面完成：在消息中 @扣子，把提示词与事件 JSON 一并发送。"),
    ("p", "3.3 实现步骤"),
    ("p", "1）本地生成事件 JSON；2）在 Coze 项目中 @扣子并发送设计文档 7.3 节的提示词全文；3）依次发送三份代表性 JSON（一级事件、二级事件、无事件）；4）记录输出并逐条核对是否遵守 8 条约束。"),
    ("p", "需要说明的差异：对话界面下提示词是作为消息内容发送的，而非配置在大模型节点的系统提示词栏，原设计的温度参数（0.3）也无法设置。因此本次验证证明了该提示词能诱导出符合规则的行为，但不能等同于工作流 LLM 节点在固定系统提示词与温度下的表现。"),
    ("p", "3.4 输入输出"),
    ("p", "输入为事件 JSON，字段为 session_id、audio_file、duration_sec、max_level 与 events 数组；events 每项含 event_id、name_cn、level、level_name、confidence、audio_set_labels。输出为 1 至 3 句中文提示文案。"),
    ("p", "三组实测结果："),
    ("p", "样本 1（max_level=1，五个事件：闹钟、电子提示音、蜂鸣器、警报器、火警）：「听到了闹钟和电子提示音的声音，可能还有蜂鸣器和报警器，也有点像火警的动静。建议您留意一下周围，确认有没有需要警惕的情况，注意安全。」"),
    ("p", "样本 2（max_level=2，单个事件：狗叫声，置信度 0.11）：「好像有狗在叫，但不太确定，您可以留意一下。」"),
    ("p", "样本 3（max_level=0，events 为空）：「当前未检测到需提示的声音。」"),
    ("p", "3.5 注意事项"),
    ("p", "提示词必须明确禁止推测未出现的声音，否则大模型会把「检测到门铃」扩写成「有人来访，请开门」这类未经检测的推断。低置信度事件须用「可能」等措辞弱化。面向听障用户应避免「置信度」「模型」等术语。链路衔接为人工粘贴，未调用 Coze API。"),
    ("blank", ""),

    ("p", "五、实验结果与分析"),
    ("p", "5.1 管线连通性验证"),
    ("p", "用 6 个合成信号（440 Hz 正弦、1000 Hz 正弦、扫频、白噪声、静音、脉冲串）在阈值 0.2 下运行检测：440 Hz 识别为电话铃声 0.97、警报器 0.81；1000 Hz 识别为电子提示音 0.98；扫频识别为警报器 0.21；白噪声、静音、脉冲串均无提示事件。440 Hz 被识别为电话铃声具有合理性——电话拨号音的标准频率正是 440 Hz，说明模型确实依据频谱特征判断而非随机输出。合成信号仅用于验证管线连通性，不能用于评估准确率。"),
    ("p", "5.2 映射覆盖率分析"),
    ("p", "对全部 521 个类别执行映射：一级 23 类（4.4%），二级 30 类（5.8%），三级可忽略 94 类（18.0%），未覆盖 374 类（71.8%）。"),
    ("p", "这是本次实验最重要的发现之一：即便放宽关键词匹配，仍有 71.8% 的类别无法归入任何听障提示场景。这并非映射表编写不足，而是 AudioSet 本体设计取向所致——它面向通用音频事件识别，包含大量乐器、音乐流派、动物物种等与听障安全无关的类别。这个数字量化了零样本路线的能力上限：系统的可提示范围被 AudioSet 本体预先决定，无法通过调参突破。像烟雾报警器、门铃这类关键声音在本体中有对应类别因而可用，但燃气报警、水壶鸣笛等更多居家安全声音没有独立类别，只能落入未覆盖。这是技术路线的结构性局限，不是实现缺陷。"),
    ("p", "5.3 事件级命中率"),
    ("p", "使用 ESC-50 抽样的 65 条片段（13 类 × 5 条）评估。指标口径说明：ESC-50 的类别体系与 AudioSet 不同，无法做标准分类准确率对比，本实验统计的是「零样本预测是否落进该类别应有的听障事件族」，即事件级命中率，不可与文献中的分类准确率直接比较。当前参数（阈值 0.2）下事件级命中率为 60.0%（39/65）。"),
    ("p", "5.4 阈值与去抖参数对比"),
    ("p", "阈值扫描（min_frames = 1）：0.05 → 76.9%（50/65），0.10 → 70.8%（46/65），0.20 → 60.0%（39/65），0.30 → 53.8%（35/65），0.50 → 43.1%（28/65）。命中率随阈值单调下降，与 2.5 节的预判一致。0.5 这一在分类任务中常见的默认阈值，会使命中率比 0.05 低 33.8 个百分点。"),
    ("p", "去抖窗口扫描（阈值固定 0.10）：1 帧 → 70.8%，2 帧 → 72.3%，3 帧 → 64.6%。窗口从 1 帧放宽到 2 帧时命中率略有提升，说明双帧一致性确实滤除了部分瞬时误报；继续放宽到 3 帧则明显下降，因为 5 秒片段仅含约 10 帧，要求 3 帧命中会滤掉真实但短暂的声音事件。"),
    ("p", "参数选择建议：若以召回优先，取阈值 0.05 至 0.10、min_frames = 2；若以抑制误报优先，取阈值 0.20 至 0.30。本实验认为听障安全场景应以召回优先——漏报火警的代价远高于多提示一次门铃。参数扫描对比图如下："),
    ("image", "chart"),
    ("p", "5.5 误判案例分析"),
    ("p", "在低阈值下，闹钟片段 5-210612-A-37 触发了一级事件「火警」（置信度 0.34）与「警报器」（0.38），同片段同时给出二级事件「闹钟」（0.55）。这是典型误报：闹钟的周期性蜂鸣在频谱特征上与火警报警声高度相似。该案例说明两点：一是低阈值以误报率换召回率，需按场景权衡；二是 AudioSet 中 Alarm、Buzzer、Beep、Alarm clock、Fire alarm 等类别声学特征相互交叠，仅靠类别映射难以完全区分。此外，部分片段（如敲门声、玻璃破碎）在零样本设置下未被检出，原因是这些声音在训练分布中样本较少。这类漏报在安全场景中后果严重，是本路线最需警惕的失败模式。"),
    ("p", "5.6 Coze 提示层验证结果"),
    ("p", "逐条核对三组输出对提示词 8 条规则的遵守情况。符合的部分：规则 1（只依据 events 陈述、不推测）三组均未出现 JSON 之外的声音；规则 3（置信度低于 0.5 用「可能」）样本 1 中三个低置信度事件用「可能」「有点像」弱化、两个高置信度事件直述，样本 2 的 0.11 用「好像」「不太确定」，区分正确未一刀切；规则 4（多事件全部提及）样本 1 的 5 个事件无遗漏；规则 6、7、8 均符合。"),
    ("p", "两处偏差。一是规则 5 指定的固定字符串「当前环境未检测到需要提示的声音。」未被照抄，实际输出为「当前未检测到需提示的声音。」，语义一致但措辞不同——固定文案不应交给大模型自由生成，应改为代码层兜底。二是规则 2 对 max_level=1 要求「语气明确、直接」，样本 1 实际表述偏谨慎（「有点像火警的动静」「建议您留意一下」）。考虑到该样本五个事件的置信度全部低于 0.55，软化处理在情理上更稳妥，但与提示词字面要求有距离。"),
    ("p", "一个系统级发现：样本 1 正是 5.5 节记录的误报案例，而 Coze 输出忠实地复述了这个误报。这暴露了端到端链路的结构性风险：提示词第 1 条「只依据 events 陈述、不推测」在保护用户免受模型幻觉误导的同时，也剥夺了大模型质疑上游检测结果的权限。检测层一旦出错，提示层会以可信的语气把错误传达给用户，而用户无从分辨。结论：大模型提示层不能作为精度兜底，误报抑制必须在检测层通过阈值与去抖参数解决。验证过程记录截图如下："),
    ("image", "coze"),
    ("blank", ""),

    ("p", "六、实验伦理与风险讨论"),
    ("p", "1. 漏报与误报的后果不对称。系统输出直接影响听障用户对环境的判断，漏报火警可能导致人身伤害，误报则在长期使用中造成提示疲劳，使用户忽略真实警报。工程上必须明确设计取向，并在产品说明中如实告知能力边界。"),
    ("p", "2. 持续监听的隐私风险。真实部署需麦克风持续采集环境音频，可推断用户在家时长、作息、来访情况等敏感信息。必须做到本地推理、音频不外传不留存。本实验为离线批处理，不涉及此问题。"),
    ("p", "3. 数据集与模型的偏见。AudioSet 的类别分布以欧美城市环境为主，对特定地区覆盖不足，5.2 节 71.8% 的未覆盖率正反映了这一点。直接部署可能出现在特定居住环境中大量关键声音无法识别的情况，且这种失效是静默的——用户不会知道系统「听不到」什么。"),
    ("p", "4. ESC-50 数据许可与署名。本实验使用的 ESC-50 采用 CC BY-NC 3.0（署名—非商业性使用）许可，仅用于课程教学，不商用、不再分发，署名原作者 Piczak, K. J.。任何商业用途均需另行获得授权。"),
    ("p", "5. 责任边界。本实验为技术验证，不具备任何安全设备资质，不可作为火灾报警、安防监控的替代或补充。若用于真实辅助，需明确其为「辅助提示」而非「安全保障」。"),
    ("blank", ""),

    ("p", "七、实验提交要求"),
    ("p", "1、实验报告选用通用模板，写清详细的搭建过程，可配图和代码，对实验结果有一定的分析。"),
    ("p", "2、实验成果，录运行演示视频，并提交输出结果。如工作流、软件等可访问，则提供链接或者打包代码。"),
    ("p", "本项目全部代码、实验数据与报告已托管至 GitHub 公开仓库，可直接访问："),
    ("link", "", "github"),
    ("p", "仓库包含 11 个功能模块与 10 个测试文件（共 65 个单元测试）、ESC-50 抽样的完整评估产物（命中率明细、按类汇总、混淆矩阵、参数扫描图），以及本报告的设计文档与实施计划。模型权重与音频素材因体积与许可原因未纳入仓库，可分别由 download_model.py 与 download_samples.py 重新获取。"),
    ("blank", ""),

    ("p", "八、思考题"),
    ("p", "1. 本次开发有什么技术局限或者伦理风险？"),
    ("p", "技术局限主要有三。其一，零样本路线决定了能力上限：系统能识别什么完全由 AudioSet 的 521 类预先决定，实测 71.8% 的类别无法映射到听障场景，且燃气报警、水壶鸣笛等居家安全声音在本体中没有独立类别，无法通过调参弥补。其二，Alarm、Buzzer、Beep、Fire alarm 等类别声学特征高度重叠，仅靠类别映射难以区分，实测出现过闹钟被误报为火警的案例。其三，零样本模式下部分关键声音单类得分偏低而被漏检，这类漏报若不处理会严重削弱系统可信度。"),
    ("p", "伦理风险方面，最突出的是漏报与误报后果的不对称性，以及长期误报导致的提示疲劳；其次是持续麦克风采集带来的隐私暴露；第三是数据集地域偏见造成特定环境下静默失效，而用户对此无从知晓。因此应当把系统定位为辅助提示而非安全保障。"),
    ("p", "2. 你还能想到哪些包含音频处理模块的应用？"),
    ("p", "一是工业设备预测性维护，通过对机械运转声的异常检测提前发现故障。二是城市噪声监测与声环境地图，用分布式麦克风节点绘制噪声时空分布。三是野生动物声学监测，通过识别物种鸣叫评估生物多样性。四是居家养老看护，识别跌倒声、呼救声等异常。五是会议与课堂的说话人分离与纪要生成。六是智能耳机的通透模式，根据环境声类型动态调整降噪策略。"),
    ("p", "3. 你想重点学习哪些数字音频处理技术？"),
    ("p", "其一，时频分析与特征提取，包括短时傅里叶变换、梅尔滤波器组、MFCC，希望从「直接调用预训练模型」进一步下沉到自己实现特征提取，理解模型的输入到底是什么。其二，数据增强与少样本学习，用于解决本次暴露的漏检问题。其三，时序建模与声事件起止检测，本次只做了片段级分类，无法给出事件在时间轴上的准确边界。其四，模型轻量化与端侧部署，若要真正做成便携辅具，需进一步量化与优化。"),
    ("blank", ""),
]
